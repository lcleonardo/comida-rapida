server:
   port: 8083
   servlet:
      context-path: /venta-comida-domicilio
      session:
         timeout: 21600s

spring:
   datasource:
      driver-class-name: com.mysql.jdbc.Driver
      type: com.zaxxer.hikari.HikariDataSource
      jdbcUrl: jdbc:mysql://localhost:3306/comida-rapida
      username: root
      password: root
      hikari:
         poolName: pool
